// Playground - noun: a place where people can play

import Cocoa

// That's simple to generate CSV strings
let userInfo = ["Sarath", "30", "Experion Technologies", "9686233235"]
let printable = ",".join(userInfo)

/*
+= creates another array
first and last returns optional. it can be nil

append, insert, splice


removeAt, removeRange, replaceRange
it doesn't need to be on same length
sort - it can be a closure

filter - 
maps - 
reduce - 
*/


